# Bash Backup Automation Script

Basic automation project created using Bash in WSL (Ubuntu/Linux).

## Features
- Automatically creates folder backups
- Uses rsync for synchronization
- Creates log entries with timestamps
- Automates repetitive backup tasks

## Technologies Used
- Bash
- Linux
- WSL
- rsync

## Run Script

```bash
chmod +x backup.sh
./backup.sh
